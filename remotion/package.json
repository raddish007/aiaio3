{
  "name": "aiaio3-remotion",
  "version": "1.0.0",
  "private": true,
  "main": "src/Root.tsx",
  "scripts": {
    "build": "remotion build src Root --out=dist",
    "preview": "remotion preview src Root",
    "lambda:deploy": "remotion lambda functions deploy",
    "lambda:functions": "remotion lambda functions list",
    "lambda:logs": "remotion lambda functions logs"
  },
  "dependencies": {
    "@remotion/lambda": "^4.0.322",
    "@remotion/transitions": "^4.0.322",
    "remotion": "^4.0.322"
  }
}
